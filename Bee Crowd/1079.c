#include <stdio.h>
int main(void)
{
    double f1, f2, f3, f4;
    double num[2];
    for (int i = 0; i < a; i++)
    {
        scanf("%lf", &num[i]);
    }

    f1 = num[0] * 2;
    f2 = num[1] * 3;
    f3 = num[2] * 5;
    f4 = (f1 + f2 + f3) / 10;
    printf("%.1lf\n", f4);
    return 0;
}
