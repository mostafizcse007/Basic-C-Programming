#include <stdio.h>
int main(void)
{
    int i, j;
    double k = 0.2, l, m;
    for (i = 0; i <= 2; i++)
    {
        for (j = l; j <= 3; j++)
        {
            printf("I=%d J=%d\n", i, j);
        }
        l += k;
        m += k;
    }
    return 0;
}
